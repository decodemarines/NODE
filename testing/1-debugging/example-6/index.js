const months = ['January', 'February', 'March', 'April'];

// Need to create breakpoint
// 1. Simple
// 2. Expression
// 3. Condition
// 4. Log message
for (let i = 0; i < months.length; i++) {
    const month = months[i];
    // do some code
    const monthUpper = month.toUpperCase();
}
